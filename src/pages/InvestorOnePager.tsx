import { WorkflowPage } from "@/components/features/WorkflowPage";

const InvestorOnePager = () => {
  return (
    <WorkflowPage
      workflowType="investor-one-pager"
      title="Investor/Partner One-Pager"
      description="Create a concise one-pager, pitch deck outline, and multiple elevator pitch versions"
      exampleCommand="Generate pitch materials for investor conversations"
    />
  );
};

export default InvestorOnePager;

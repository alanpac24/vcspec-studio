import WorkflowPage from "./WorkflowPage";

const FeedbackLoop = () => {
  return (
    <WorkflowPage
      workflowType="feedback-loop"
      title="Feedback Loop Setup"
      description="Build structured channels for collecting user feedback"
      exampleCommand="Set up feedback collection from my users"
    />
  );
};

export default FeedbackLoop;
